default install
label install
kernel ubuntu-installer/amd64/linux
APPEND root=/dev/ram0 ramdisk_size=1500000 ip=dhcp url=http://{{ ansible_eth1.ipv4.address }}/tftp/ubuntu-20.04-live-server-amd64.iso autoinstall ds=nocloud-net;s=http://{{ ansible_eth1.ipv4.address }}/tftp/
EOF